{#
/**
 * @file
 * Default theme implementation for a branding block.
 *
 * Each branding element variable (logo, name, slogan) is only available if
 * enabled in the block configuration.
 *
 * Available variables:
 * - site_logo: Logo for site as defined in Appearance or theme settings.
 * - site_name: Name for site as defined in Site information settings.
 * - site_slogan: Slogan for site as defined in Site information settings.
 *
 * @ingroup themeable
 */
#}
<div class="usa-logo site-logo grid-row flex-align-center flex-justify-center grid-gap-1" id="logo">

{% if site_logo %}
   <div class="grid-col-1">
    <a class="logo-img" href="{{ path('<front>') }}" accesskey="1" title="{{ 'Home'|t }}" aria-label="Home">
      <img src="{{ site_logo }}" alt="{{ 'Home'|t }}" />
    </a>
  </div>
{% endif %}


  <em class="usa-logo__text grid-col-auto">
  {% if site_name %}
      <a href="{{ path('<front>') }}" accesskey="1" title="{{ 'Home'|t }}" aria-label="Home">
        {{ site_name }}
      </a>
  {% endif %}

  {% if site_slogan %}
    <h2 class="font-serif-2xs text-light">{{ site_slogan }}</h2>
  {% endif %}

  </em>

</div>
